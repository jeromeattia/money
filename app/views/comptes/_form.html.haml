.row
  .small-4.offset-1.columns
    = form_for @compte  do |f|
      - if @compte.errors.any?
        #error_explanation
          %h2
            = pluralize(@compte.errors.count, "error")
            empéchent de sauvegarder:
          %ul
            - @compte.errors.full_messages.each do |msg|
              %li= msg
      .field
        = f.label :mois
        %br/
        = f.text_field :mois, :data_date_format =>"mm/dd/yy", :id=>"date-select1"
      .field
        = f.label :nom
        %br/
        = f.text_field :nom, :value => "Attia"
      .field
        = f.label :valeur
        %br/
        = f.text_field :valeur
      .actions
        = f.submit "Ajouter", :class => "button"